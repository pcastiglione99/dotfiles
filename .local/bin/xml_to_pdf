#!/bin/sh

INPUT="$1"
XML_OUTPUT=$($1| sed 's/\.xml/\.html/') 

STYLE="$HOME/.local/bin/FoglioStileItalwork.xsl"

xsltproc $STYLE $1 > $XML_OUTPUT 

html_to_pdf.js $XML_OUTPUT $2

#rm $(echo $1 | sed 's/\.xml/\.html/')
